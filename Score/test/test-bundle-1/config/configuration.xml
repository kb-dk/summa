<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<!-- Test configuration -->
<properties>

    <!-- Instance Id -->
    <entry key="summa.score.service.id">test-1-1</entry>

    <!-- The RMI communications port -->
    <entry key="summa.score.service.port">2769</entry>

    <!-- The port on which the registry should/does run -->
    <entry key="summa.score.service.registry.port">2767</entry>

    <!-- The class implementing the Service interface -->
    <entry key="score.launcher.service.class">dk.statsbiblioteket.summa.score.service.SimpleTest</entry>

</properties>